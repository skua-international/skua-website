# GitHub PAT with read access to skua-international/certifications (private repo)
GITHUB_TOKEN=

# Optional: shared secret for POST /api/docs/refresh (e.g. from GitHub Actions webhook)
# REFRESH_SECRET=
